{
  "name": "shepstack",
  "version": "0.1.0",
  "private": true,
  "description": "Write a spec. Get a full-stack app. The fastest way for non-technical founders to build production applications.",
  "repository": "https://github.com/Radix-Obsidian/ShepStack",
  "homepage": "https://github.com/Radix-Obsidian/ShepStack",
  "bugs": "https://github.com/Radix-Obsidian/ShepStack/issues",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "dev": "pnpm -C examples/budget-dashboard dev"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
