{
  "name": "shepstack",
  "version": "0.0.1",
  "private": true,
  "description": "The Shep language stack: a vertical-slice, spec-first language ecosystem",
  "type": "module",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "dev": "pnpm -C examples/budget-dashboard dev"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
