#!/bin/bash
# Run the SupportAI backend
# Generated by Shep Compiler

# Check if virtual environment exists
if [ ! -d "venv" ]; then
    echo "📦 Creating virtual environment..."
    python3 -m venv venv
fi

# Activate virtual environment
source venv/bin/activate

# Install dependencies
echo "📦 Installing dependencies..."
pip install -r requirements.txt

# Check for API key
if [ -z "$ANTHROPIC_API_KEY" ]; then
    echo ""
    echo "⚠️  Warning: ANTHROPIC_API_KEY not set"
    echo "   AI features will not work without an API key."
    echo "   Set it with: export ANTHROPIC_API_KEY=your_key_here"
    echo ""
fi

# Run the server
echo ""
echo "🚀 Starting SupportAI API server..."
echo "   API: http://localhost:3001/api"
echo "   Docs: http://localhost:3001/docs"
echo ""

# Run as module so imports work correctly
cd ..
python -m generated.main
