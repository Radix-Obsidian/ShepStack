/**
 * Shep Language Server Protocol implementation
 *
 * Provides LSP support for Shep language files.
 * This is a skeleton that will be expanded in Phase 3.
 */

// Placeholder for LSP server
// Full implementation coming in Phase 3

export const lspServer = {
  start: () => {
    console.log("Shep LSP server starting (skeleton mode)");
  },
  stop: () => {
    console.log("Shep LSP server stopped");
  },
};
